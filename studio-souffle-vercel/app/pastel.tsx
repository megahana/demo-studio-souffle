'use client';
import {ShaderGradient,ShaderGradientCanvas} from '@shadergradient/react';
import {Component,useEffect,useRef,useState} from 'react';
class Safe extends Component<{children:React.ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return {failed:true}}render(){return this.state.failed?null:this.props.children}}
export default function Pastel(){const ref=useRef<HTMLDivElement>(null);const[visible,setVisible]=useState(true);const[supported,setSupported]=useState(false);useEffect(()=>{try{const c=document.createElement('canvas');const gl=c.getContext('webgl2');if(gl){setSupported(true);gl.getExtension('WEBGL_lose_context')?.loseContext()}}catch{}},[]);useEffect(()=>{const o=new IntersectionObserver(([e])=>setVisible(e.isIntersecting));if(ref.current)o.observe(ref.current);return()=>o.disconnect()},[]);return <div className="pastel" ref={ref} aria-hidden="true"><Safe>{visible&&supported&&<ShaderGradientCanvas style={{width:'100%',height:'100%'}} pixelDensity={1} pointerEvents="none" powerPreference="low-power"><ShaderGradient control="props" type="plane" animate="on" uSpeed={0.04} loop="on" loopDuration={24} uStrength={1.2} uDensity={1.1} uFrequency={1.2} color1="#F3EDE4" color2="#A3B5A0" color3="#D9A796" lightType="3d" brightness={1.1} grain="off" cDistance={3.6} cPolarAngle={90} cAzimuthAngle={180} rotationX={0} rotationY={0} rotationZ={20}/></ShaderGradientCanvas>}</Safe></div>}
